enum NewsType {
  topTrending,
  allNews,
}

enum SortByType {
  revelancy, //articles more closely related to the query come first.
  popularity, //articles from popular sources and publishers come first.
  publishedAt, //newest articles come first.
}
